// Feature flags for toggling app capabilities.
//
// MARKETPLACE_ENABLED controls all marketplace-related UI surfaces
// (pages, nav links, action buttons, credits, selling, payouts).
// Set to `true` to re-enable the marketplace everywhere. No other
// code changes are required to flip this on or off.
export const MARKETPLACE_ENABLED = false;
